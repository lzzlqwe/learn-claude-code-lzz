# 《Qoder 上手实践：5 个配置让它真正开始干活》写作大纲（精简版）

> 本文件是文章大纲，不是正文。
>
> **定位约束**：只讲 Qoder 自身的功能与用法，全文不出现其他 AI 编程产品名称，不做跨产品对比，不引用任何代码仓库作为素材。
>
> **范围原则（重要）**：只写我亲自配过、跑通过的 5 件事。没实测的能力一律不展开，只在结尾放一张速查表 + 官方文档链接。**宁可覆盖面小，也不写没验证过的结论。**

---

## 0. 文章元信息

| 项 | 内容 |
|---|---|
| 标题候选 A | 《Qoder 上手实践：5 个配置让它真正开始干活》 |
| 标题候选 B | 《从"问一句改一处"到融入日常研发：我给 Qoder 做的 5 个配置》 |
| 目标读者 | 已经在用 Qoder 做日常编码，但没配置过任何东西的同学 |
| 全文篇幅 | **约 6000 字，10 分钟读完** |
| 章节数 | 5 章正文 + 1 章收尾 |
| 实测成本 | **6 项，一个下午可以全部跑完**（见第 8 节） |
| 核心承诺 | 读完能照着配出：一套项目规范 + 一道安全闸门 + 一种并行方式 + 一个定时巡检 |

### 0.1 开篇必须写的三段（约 400 字）

1. **我的场景**：日常改代码时反复交代同样的项目约定、担心它误跑危险命令、多任务时改动互相覆盖。这三件事催生了下面 5 个配置。
2. **范围声明**（这段是文章可信度的来源，一定要写）：本文只覆盖我实际配置并验证过的能力；Qoder 还有子代理、MCP、插件、Plan、Goal、Headless、Agent Teams、Quest 等更多能力，我在结尾给了入口和文档，但**没实测的部分不在本文展开**。
3. **时效声明**：文中开关、默认值、上限以撰写时版本为准（写明版本与日期），产品迭代较快，落地前建议对照官方文档确认。

### 0.2 开篇的总览表（正文第一张表）

| # | 配的是什么 | 解决什么问题 | 配置成本 |
|---|---|---|---|
| 1 | 项目说明与规则文件 | 不用每次重复交代约定 | 20 分钟 |
| 2 | 权限规则三档 | 危险操作做不了 | 10 分钟 |
| 3 | 一个 PreToolUse hook | 团队规范变成硬约束 | 30 分钟 |
| 4 | Worktree 并行 | 多任务不互相覆盖 | 5 分钟 |
| 5 | 一个定时任务 | 巡检不用人推 | 5 分钟 |

---

## 1. 第 1 章：让它记住项目约定（约 1600 字 · 本文最重要一章）

**问题**：每开一个新会话，构建命令、目录结构、代码风格都要重讲一遍。

**内容点（只写这些，不铺开讲全部加载逻辑）**

- 两类记忆的分工：**静态记忆**（你写的项目说明与规则文件，稳定约定）vs **自动记忆**（Qoder 从对话里自己保存的偏好与背景）。本章重点写静态记忆——因为它是你能完全控制的那部分。
- 一句必须写的边界：**记忆是给模型的上下文，不是强制策略。要硬性阻止某类操作，用权限或 Hooks（第 2、3 章）。**
- 四个位置及"该不该提交"：
  - `~/.qoder/AGENTS.md` — 个人跨项目偏好，不提交
  - `<project>/AGENTS.md` — 团队共享，提交
  - `<project>/AGENTS.local.md` — 本机私有（本地服务地址等），不提交
  - `<project>/.qoder/rules/**/*.md` — 按主题拆分的规则，提交
- `/init` 可以直接生成项目说明文件的初始版本，`/memory` 查看当前加载了哪些。
- **规则的四种生效方式**（本章核心价值，每种给一个 frontmatter 示例，共 4 段代码）：
  - 始终生效：不写 frontmatter，或 `trigger: always_on`
  - 手动引入：`trigger: manual`
  - 模型决策：`trigger: model_decision` + 必填非空 `description`（只注入路径和描述，模型判断相关时才读正文）
  - 指定文件生效：`trigger: glob` + `glob`（访问匹配文件后才加载）
- 一条实用细节：会话中修改规则文件，下一轮就会被感知，不用重启。
- 自动记忆只用两句话交代：默认可能未开启，`/memory` 可以查看和管理；它是本机文件、不随代码提交、可能过期。**不展开配置。**

**必须给出的可复制片段（3 个）**

1. 一份 `AGENTS.md` 模板：构建/测试/格式化命令、目录结构与模块边界、代码风格与评审要求、禁止改动的生成目录
2. 一条 `always_on` 通用约定规则
3. 一条 `glob` 目录级规则（例如只对接口层生效）

**坑与建议**
- 不适合写进项目说明的内容：临时状态、会过期的排期、能从代码直接看出的长篇内容、必须强制执行的安全策略。
- **指令越具体越稳定**，给一组"笼统 → 具体"的改写对照（例如"注意代码质量" → "提交前运行 `<测试命令>`"）。
- 不遵守时的排查三步：`/memory` 里有没有这个文件 → 当前目录是否为可信工作区（未信任目录不会加载项目设置、Hooks、MCP 和项目说明）→ 多个文件之间是否有冲突指令。
- 安全提醒：这些文件会随仓库提交，不要写密钥、内部地址、账号信息。

**文档**：`https://docs.qoder.com/zh/cli/memory`

---

## 2. 第 2 章：给它划一条安全线（约 800 字）

**问题**：想放手让它干活，又怕一条命令毁掉工作区。

**内容点（只写权限，不写沙箱——沙箱实测成本高，放到结尾速查表）**

- 判定模型：每次工具调用前检查一次，结果只有 `allow` / `ask` / `deny` 三种，具体行为由权限模式决定。
- **可见 ≠ 可执行**：`--tools` 控制"它能看到哪些工具"，`--allowed-tools` / `--disallowed-tools` 控制"允许不允许执行"。这两件事分开，是最容易配错的点。
- 持久化配置：`settings.json` 里的 `permissions.allow` / `permissions.ask` / `permissions.deny`。
- 交互内查看与调整：`/permissions`。
- 一个必须提醒的行为差异：**非交互场景下 `ask` 会被当作拒绝**。所以想让它自动跑之前，权限必须先配好，否则表现是"什么都没做就结束了"。

**必须给出的可复制片段（2 个梯度，别给三个，够用就行）**

1. 只读探索档（一行命令，适合让它摸陌生代码而不改任何东西）
2. 日常开发档的 `settings.json` 骨架：常规只读与状态检查 `allow`、敏感操作 `ask`、破坏性命令 `deny`

**坑与建议**
- 先从"只读档"开始用几天，再逐步放开，比一上来全放开更容易建立信任。
- 规则里带参数的写法（如只允许某个具体子命令）覆盖范围要自己验一次再写进文章。

**文档**：`https://docs.qoder.com/zh/cli/permissions`

---

## 3. 第 3 章：一个 Hook 把规范变成硬约束（约 1300 字 · 现场演示效果最好的一章）

**问题**：规范写在文档里没人执行；写在项目说明里模型可能不遵守。需要一层不可绕过的机制。

**内容点（只讲 `command` 类型，其余三种类型放结尾速查表提一句）**

- Hooks 的定位：在特定时机自动执行你自己的逻辑，可以**拦住**工具调用。
- **只介绍 4 个事件**（不列全部 27 个，避免变成文档搬运）：
  - `PreToolUse` — 工具调用前，可拦截
  - `PostToolUse` — 工具调用成功后，做副作用
  - `SessionStart` — 会话开始，注入上下文
  - `Stop` — 主 Agent 停止响应时
  - 补一句："完整事件清单见官方文档，本文只用到这几个。"
- 配置结构：写在 `settings.json` 的 `hooks` 分组，事件 → 若干组，每组含 `matcher` + `hooks` 数组。
- `matcher` 写法：空或 `*` 全匹配、精确值、`|` 多值（如 `Write|Edit`）、正则。
- 退出码契约（本章的关键知识）：
  - `0` = 成功放行
  - `2` = **阻塞**，stderr 内容会作为反馈返回给 Agent（并非所有事件都支持阻塞）
  - 其他 = 非阻塞错误，记录但不中断
- 单条 hook 的两个实用字段：`timeout`（必设）、`async`（后台执行不阻塞主流程，适合格式化这类副作用）。
- 提一句进阶方向不展开：除了执行 shell，hook 还支持 HTTP 请求、以及用一次模型调用来做判定——本文只用最基础的 shell 类型。

**必须给出的可复制片段（只给 2 个配方，别给 4 个）**

1. **危险命令拦截**：`PreToolUse` + `matcher: "Bash"` + 一个十几行的检查脚本，命中就 `exit 2` 并向 stderr 写明原因
2. **改完文件自动检查**：`PostToolUse` + `matcher: "Write|Edit"` + `async`，跑格式化或 lint

**坑与建议**
- hook 脚本必须快、必须幂等，`timeout` 一定要设。
- 目录未被信任时 Hooks 不会加载——排查先看这一条。
- 用 `exit 2` 时 stderr 要写清"为什么被拦 + 应该怎么做"，因为这段话是给 Agent 看的，写清楚它会自己换做法。

**文档**：`https://docs.qoder.com/zh/cli/hooks-reference`

---

## 4. 第 4 章：多任务并行不互相覆盖（约 900 字）

**问题**：同时推进两个任务，共享一个目录必然互相覆盖改动。

**内容点**

- `qoder --worktree <名称>`，也可以直接带上任务描述一起启动；不传名称会自动生成。
- 目录与分支：命名 worktree 放在 `<repo>/.qoder/worktrees/<处理后的名称>` 并创建对应临时分支；**同名已存在时直接复用，不会重复创建**。
- 前提：在 git 仓库中运行且本地已装 git。
- 新 worktree 是干净检出，**默认不含未跟踪文件**；需要 `.env` 这类本地文件时，用仓库根目录的 `.worktreeinclude` 声明。
- 退出时的安全检查：会检查未提交文件与新增提交，干净的可以自动删除，有内容时让你选择保留还是删除。
- 手动清理：`git worktree remove <路径>` + 删除临时分支。

**必须给出的可复制片段**：双终端并行的完整示例（终端 1 改模块 A、终端 2 改模块 B）+ 一份 `.worktreeinclude` 示例。

**坑与建议**
- 一任务一 worktree，别在一个里面塞多件事。
- 用完及时清理，否则仓库里会堆一堆临时分支。
- 提一句不展开：批量跨文件改动有专门的内置能力，子代理也可以声明在独立检出里工作——本文只写手动并行这一种。

**文档**：`https://docs.qoder.com/zh/cli/parallel-tasks`

---

## 5. 第 5 章：让巡检不用人推（约 700 字）

**问题**："每天早上看一眼 CI""每周汇总一次提交"这类事，靠人记必然漏。

**内容点**

- 创建方式很轻：直接用自然语言描述时间和要做的事，它会解析成 cron 表达式并建任务。给 2–3 个自然语言示例。
- 两种类型：一次性（触发后自动删除）、周期（按 cron 反复触发）。
- 标准 5 字段 cron（分/时/日/月/星期），按**运行环境本地时区**解释。给一张 4 行的对照表（每 5 分钟 / 每天 9:00 / 工作日 9:00 / 每周一 10:00）。
- 落盘位置：`<project>/.qoder/scheduled_tasks.json`。
- **必须写全的三条限制**（读者最容易踩）：
  - 任务数量上限 50
  - **周期任务创建后 7 天自动过期**，需要长期跑就得重建
  - 最小粒度 1 分钟，且触发时间带抖动（避免集中触发），所以不要用它做要求精确到秒的事
- 进程没运行时错过的任务，启动时会提示，由你决定是否补跑。
- 查看与删除：让它列出任务、按 id 删除。

**坑与建议**
- 7 天过期这条会让"长期巡检"悄无声息地失效，写清应对思路。
- 定时任务触发的动作会真的执行——先用只读类任务（汇总、检查）验证，别一上来就让它自动改代码。
- 提一句不展开：还有按固定间隔重复运行的方式，以及在脚本/CI 里跑的非交互模式。

**文档**：`https://docs.qoder.com/zh/cli/scheduled-tasks`

---

## 6. 第 6 章：收尾（约 700 字）

### 6.1 还有哪些能力（速查表，只给入口和文档，明确说明未实测）

这张表是"诚实的收尾"，同时给读者继续探索的路：

| 能力 | 一句话 | 入口 | 文档 |
|---|---|---|---|
| 沙箱 | 在隔离环境里执行命令，可限制文件与网络 | `--sandbox` | `/zh/cli/sandbox` |
| 子代理 | 把一类任务交给有独立上下文和专属工具集的代理 | `/agents` | `/zh/cli/subagent` |
| Skills | 把成熟工作流封装成可复用能力 | `/skills` | `/zh/cli/built-ins` |
| MCP | 接入外部系统的工具，走同一套权限 | `/mcp` | `/zh/cli/mcp-servers` |
| 插件 | 把规范、工具、技能打包分发给团队 | `/plugins` | `/zh/cli/plugins-reference` |
| Plan | 先只读出方案，确认后再改代码 | `/plan` | `/zh/cli/plan-mode` |
| 任务与看板 | 跟踪多步骤任务的归属、状态与依赖 | `/tasks` `/kanban` | `/zh/cli/built-ins` |
| Goal | 设定完成条件，让它持续做到达成 | `/goal set` | `/zh/cli/goal` |
| 非交互模式 | 在脚本或流水线里运行 | `--print` | `/zh/cli/run-in-scripts` |
| 上下文管理 | 主动压缩、查看上下文构成、回退检查点 | `/compact` `/context` `/rewind` | `/zh/cli/sessions` |
| IDE 任务工作台 | 描述目标、跟踪状态、审查 Diff 后提交 | Quest 视窗 | `/zh/user-guide/quest/overview` |

表下面写一句：**以上我尚未系统实测，列在这里是给方向，不作结论。**

### 6.2 一个建议的落地顺序（5 天，每天一件事）

- 第 1 天：`/init` + 把构建测试命令和目录结构写进项目说明
- 第 2 天：拆 2 条规则，其中一条用 `glob` 绑定目录
- 第 3 天：配权限三档，先按只读档用一天
- 第 4 天：配一个 `PreToolUse` hook 拦危险命令
- 第 5 天：跑一次 worktree 并行 + 建一个只读巡检定时任务

### 6.3 排查索引（按症状查，5 行就够）

| 症状 | 先查什么 |
|---|---|
| 不遵守项目说明 | `/memory` 里有没有该文件、目录是否可信、多个文件是否冲突 |
| Hooks 不生效 | 当前目录是否为可信工作区、`matcher` 是否匹配、退出码是否为 2 |
| 自动跑起来"什么都没做" | 非交互下 `ask` 视为拒绝，检查权限预配置 |
| 定时任务不再触发 | 周期任务 7 天过期、进程是否在运行 |
| 某个斜杠命令看不到 | 对应能力未启用或被策略关闭 |

### 6.4 附录

- 附录 A：`settings.json` 完整示例（权限 + hooks 两部分合一）
- 附录 B：`AGENTS.md` 与规则文件模板

---

## 7. 写作规范（自我约束清单）

1. **不出现任何其他 AI 编程产品名称，不做跨产品对比。**
2. **不引用任何代码仓库作为素材**；示例统一用占位路径和占位命令。
3. **每章固定四段式**：问题 → 怎么配（可复制） → 关键参数与默认值 → 坑与建议。
4. **没实测过的不写成结论**，统一收进 6.1 速查表并注明未实测。
5. **默认值和上限一定写出来**（任务上限 50、周期任务 7 天过期、新 worktree 不含未跟踪文件等）——这是实战文章和文档搬运的分水岭。
6. 每章代码块不超过 2 段，超长配置放附录。
7. 不出现真实内部系统名、地址、密钥、账号。
8. 术语统一：工作区、项目说明文件、规则、定时任务，不混用中英。
9. 全文控制在 6000 字，写超了先砍第 5 章和 6.2，别砍第 1、3 章。

---

## 8. 实测清单（只有 6 项，一个下午完成）

| # | 实测项 | 预计耗时 | 验证方式 |
|---|---|---|---|
| 1 | 写一份 `AGENTS.md`，确认被加载并被遵守 | 30 min | `/memory` 能看到它；提一个会触发该约定的请求 |
| 2 | 写一条 `glob` 规则，确认只在访问匹配文件后生效 | 30 min | 在规则里放一个可观察的标记性要求 |
| 3 | 配权限三档，各触发一次 allow / ask / deny | 30 min | 观察是否直接执行、弹确认、被拒绝 |
| 4 | 配一个 `PreToolUse` hook，让它拦住一条命令 | 60 min | 命中时 `exit 2`，观察会话里的反馈文本 |
| 5 | 跑一次双终端 `--worktree` 并行 | 20 min | 确认目录与分支命名、退出时的清理提示 |
| 6 | 建一个"每分钟一次"的只读定时任务，等它触发 | 20 min | 确认触发、落盘文件内容、按 id 删除 |

**总计约 3.5 小时。** 全部跑通后再动笔，正文写作预计 3–4 小时。

> 特意排除的实测项：沙箱（要装容器环境）、自定义子代理、MCP、插件、Plan、Goal、非交互模式、Agent Teams、Quest 深度使用。这些都进 6.1 速查表，不写结论。

---

## 9. 组内分享映射（15 分钟版）

| 时长 | 内容 | 对应章节 |
|---|---|---|
| 2 min | 总览表：我配了哪 5 件事、各解决什么 | 第 0 章 |
| 4 min | **现场演示**：hook 拦住一条危险命令 | 第 3 章 |
| 3 min | **现场演示**：打开某目录文件后规则被加载 | 第 1 章 |
| 3 min | **现场演示**：双终端 worktree 并行改两个模块 | 第 4 章 |
| 3 min | 5 天落地顺序 + 发一份配置模板 | 第 6 章 |

分享的记忆点来自"看到它真的被拦住了"，不来自讲稿。配置模板（附录 A/B）比 PPT 更容易被同事真正用起来。

---

## 附：暂不写入本文的内容（留给后续第二篇，不要删）

如果这一篇反馈不错，下面这些可以单独成篇，实测成本明显更高，不适合放进第一篇：

- **沙箱完整配置**：多种后端、自动检测顺序、文件与网络边界、工具级沙箱开关
- **Hooks 进阶**：完整事件清单（含压缩前后、任务、工作树、权限、子代理空闲等事件）、HTTP 类型、用模型调用做语义校验的两种类型、stdout JSON 精细控制字段、插件内携带 Hooks
- **扩展体系**：内置与自定义子代理、内置 Skills 清单、MCP 接入、插件分发
- **长任务与自动化**：Plan、任务与看板、依赖关系、Goal、非交互模式与 CI 集成
- **多智能体协作**：Agent Teams（beta，需环境变量开关，团队仅存在于当前会话）
- **IDE 侧**：Quest 的两种模式、任务看板、知识中心、审查与提交流程
